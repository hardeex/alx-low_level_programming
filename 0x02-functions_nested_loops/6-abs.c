#include "main.h"

/**
 * _abs- A program that prints absolute value of an integer
 * @i: The argument of the program
 *
 * Return: 0 if successful
 */
int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}
