#include "main.h"
/**
 * add- A program that adds two integers and return the result
 * @a: The first argument
 * @b: The second argument
 * Return: 0 if successful
 */
int add(int a, int b)
{
	return (a + b);
}
